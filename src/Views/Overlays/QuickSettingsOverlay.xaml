<Window x:Class="AlctClient.Views.Overlays.QuickSettingsOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QuickSettingsOverlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        SizeToContent="WidthAndHeight">

    <Window.Resources>
        <Style x:Key="LangBtn" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bg" Background="Transparent" CornerRadius="15" Padding="8,0">
                            <TextBlock Text="{TemplateBinding Content}"
                                       Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CaptionBtn" TargetType="ToggleButton">
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="Dot" Text="○"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       FontSize="10"
                                       Margin="6,0,0,0"
                                       VerticalAlignment="Center"/>
                            <Border x:Name="Bg" Background="Transparent" CornerRadius="5" Padding="12,4" Margin="0,0,2,0">
                                <TextBlock x:Name="Label" Text="음성 번역"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           FontSize="12"
                                           VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Dot" Property="Text" Value="●"/>
                                <Setter TargetName="Dot" Property="Foreground" Value="{StaticResource TextSuccessBrush}"/>
                                <Setter TargetName="Label" Property="Foreground" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Dot" Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                <Setter TargetName="Label" Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="RootBorder"
            CornerRadius="{StaticResource PillRadius}"
            BorderBrush="{StaticResource CardBorderAccentBrush}"
            BorderThickness="1"
            Padding="4"
            MouseEnter="OnMouseEnter"
            MouseLeave="OnMouseLeave">

        <StackPanel Orientation="Horizontal">

            <!-- 항상 보이는 언어 표시 원 -->
            <Border Width="28" Height="28" CornerRadius="14"
                    Background="{StaticResource AccentBrush}"
                    VerticalAlignment="Center">
                <TextBlock x:Name="LangLabel"
                           Text="JP"
                           Foreground="White"
                           FontSize="12" FontWeight="Bold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>

            <!-- 호버 시 확장되는 메뉴 -->
            <StackPanel x:Name="ExpandedPanel" Orientation="Horizontal" Visibility="Collapsed" Margin="5,0,0,0">
                <RadioButton x:Name="BtnJa" Content="일본어" GroupName="Lang"
                             Tag="ja-JP" Style="{StaticResource LangBtn}"
                             Margin="10,0,0,0"
                             Checked="OnLangChecked"/>
                <RadioButton x:Name="BtnZh" Content="중국어" GroupName="Lang"
                             Tag="zh-CN" Style="{StaticResource LangBtn}"
                             Margin="4,0,0,0" Checked="OnLangChecked"/>
                <RadioButton x:Name="BtnEn" Content="영어" GroupName="Lang"
                             Tag="en-US" Style="{StaticResource LangBtn}"
                             Margin="4,0,0,0" Checked="OnLangChecked"/>
                <Rectangle Width="1" Fill="{StaticResource BorderBrush}" Margin="5,4"/>
                <ToggleButton x:Name="BtnCaption"
                              Style="{StaticResource CaptionBtn}"
                              Checked="OnCaptionToggled"
                              Unchecked="OnCaptionToggled"/>
            </StackPanel>

        </StackPanel>
    </Border>
</Window>
