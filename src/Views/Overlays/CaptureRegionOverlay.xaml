<Window x:Class="AlctClient.Views.Overlays.CaptureRegionOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        MinWidth="100" MinHeight="50"
        SizeChanged="OnSizeChanged"
        MouseLeftButtonDown="OnDragMove">

    <Window.Resources>
        <Style x:Key="EditBtn" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>

        <!-- 영역 외곽선 (본문 드래그로 이동) -->
        <Rectangle Stroke="{StaticResource AccentBrush}" StrokeThickness="2"
                   Fill="#90000000" Cursor="SizeAll"/>

        <!-- 상단 가이드 텍스트 -->
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,16,0,0"
                    IsHitTestVisible="False">
            <TextBlock Text="번역할 텍스트가 표시되는 영역을 지정하세요."
                       Foreground="White" FontSize="13" HorizontalAlignment="Center">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.9" Color="Black"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="브라우저 등 화면 전체도 지정할 수 있어요. '전체화면' 버튼을 눌러보세요."
                       Foreground="#BBBBBB" FontSize="11" HorizontalAlignment="Center" Margin="0,3,0,0">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.9" Color="Black"/>
                </TextBlock.Effect>
            </TextBlock>
        </StackPanel>

        <!-- 하단 크기 표시 + 버튼 + 가이드 바 -->
        <StackPanel VerticalAlignment="Bottom">
            <Border Background="{StaticResource AccentBrush}" Padding="0,5" IsHitTestVisible="False">
                <TextBlock Text="드래그하여 이동 · 가장자리를 드래그하여 크기 조절"
                           Foreground="White" FontSize="10" HorizontalAlignment="Center"/>
            </Border>
            <StackPanel HorizontalAlignment="Center" Margin="0,8,0,8">
                <TextBlock x:Name="SizeLabel"
                           Foreground="White" FontSize="11" FontFamily="Consolas"
                           HorizontalAlignment="Center" Margin="0,0,0,5">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.9" Color="Black"/>
                    </TextBlock.Effect>
                </TextBlock>
                <StackPanel Orientation="Horizontal">
                    <Button Content="취소"
                            Style="{StaticResource EditBtn}"
                            Background="#90000000" Foreground="White"
                            Margin="0,0,6,0"
                            Click="OnCancel"/>
                    <Button Content="전체화면"
                            Style="{StaticResource EditBtn}"
                            Background="#90000000" Foreground="White"
                            Margin="0,0,6,0"
                            Click="OnFullscreen"/>
                    <Button Content="저장"
                            Style="{StaticResource EditBtn}"
                            Background="{StaticResource AccentBrush}" Foreground="White"
                            Click="OnSave"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>

    </Grid>
</Window>
